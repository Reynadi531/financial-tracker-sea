# Desktop app environment variables

# The desktop app bundles the web build from apps/web and runs it in a native webview.
# It does not require additional environment variables.
# Ensure the web app is built with the correct VITE_SERVER_URL in apps/web/.env.
